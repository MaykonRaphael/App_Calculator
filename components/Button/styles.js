import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        borderColor: '#FFF',
        borderWidth: 2,
        width: '33.3%',
        height: '20%',
        justifyContent: 'center',
        borderRadius: 10,
        alignItems: 'center',
    },
    text: {
        color: '#FFF',
        fontSize: 24
    }
});